# tools/test-lab

Infrastructure for the disposable Windows VM test lab used by the system test tier
([`docs/test-strategy.md`](../../docs/test-strategy.md), `agent_start.md` section 12.3).

Will contain provisioning and reset automation for supported Windows 11 editions/builds, so
each system-test run starts from a clean, known state.

_Placeholder until the system test tier is implemented (Milestone 3+)._
