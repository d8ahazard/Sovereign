<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Sovereign.UI.Pages.RestorePointsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Sovereign.UI.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="32" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="2">
            <StackPanel Orientation="Horizontal" Spacing="12">
                <TextBlock Text="Restore points" FontSize="28" FontWeight="SemiBold" />
                <Button Click="OnRefresh" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE72C;" FontSize="14" />
                        <TextBlock Text="Refresh" />
                    </StackPanel>
                </Button>
            </StackPanel>
            <TextBlock Text="Every applied policy captured its original state here. Revert puts it back exactly as it was."
                       Opacity="0.7" TextWrapping="Wrap" />
        </StackPanel>

        <InfoBar x:Name="ResultBar" Grid.Row="1" IsOpen="False" IsClosable="True" />

        <TextBlock x:Name="EmptyText" Grid.Row="2" Text="No restore points yet. Apply something in Cleanup first."
                   Opacity="0.7" Visibility="Collapsed" Margin="0,8" />

        <ListView x:Name="PointsList" Grid.Row="3" SelectionMode="None"
                  BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="12,10" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:RestorePointRow">
                    <Grid ColumnSpacing="14">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Spacing="2">
                            <TextBlock Text="{x:Bind PolicyId}" FontWeight="SemiBold" />
                            <TextBlock Text="{x:Bind CreatedText}" Opacity="0.7" FontSize="12" />
                        </StackPanel>
                        <Button Grid.Column="1" Content="Revert" Click="OnRevert" Tag="{x:Bind PolicyId}" VerticalAlignment="Center" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
